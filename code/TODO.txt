Design Focused:
 - Voxel engine
  - Create chunks (probably look back on the way they are done in handmade hero)
  - Create forward facing geo.
   - Follow jdh guy, he had some stuff about creating an algorithm to determine if a cube was facing forward or not
 - Redo camera system such that it rotates around a fixed point using some sort of cursor movement
 - Eliminate the appearance of voxels depending on if they have been clicked
   - Raycasting system
   - Recognizing hit cubes and hiding them from being rendered
- Create scoring system based on how well the sculpture was created according to the picture
 - Wow what does this even mean I'm talking super reach right here


Quick and Easy:
 - Fullscreen mode
 


Longer, probably not as easy:
 - Redo Camera System
 - Create raycasting system (probably would help if they could be debugged too)
 - Eliminate appearance of voxels depending on if they have been clicked



Reaching now:
- Buff out backend to be used for multiple games



Well I can't say I'm super ecstatic about the fact that I just wasted so much time trying to get instancing working only to learn that it's not useful when I'm creating a voxel grid
of cubes where the faces need to be edited to reduce the amount of verts being drawn


TODO:
 - Removing instancing replace with draw calls of edited meshes
 - Introduce arc ball camera
 - Edit the mesh by clicking and removing voxels

Is empty not is solid




Take vertex_position_color and place it into the vertex buffers which means you need to create vertex buffers per object not just index buffers


Something is not right with the algo creating indices, it's inputing greater than what exists in the amount of indicies for some reason



How to determine if the cube is on the outside
 - The current index is larger than the resolution or less than 0
 - Get the neighbor and check if it's being rendered


The issue seemingly is that the indices aren't being initialized properly, this could be a couple of things
  - Not being initialized correctly to begin with (prob still the case with the locations and such not looking right)
  - Not being translated into dx11 correctly or as intended 
  - Doesn't check edge cases like the fact that the voxel at i == 60, i - 1 == 59 (which is a valid voxel and therefore won't render because it exists


It's possible that the indices aren't being properly stored because it's looking for indicies 1, 2, 3, 4 when I'm inputting indices 5, 6, 7 etc...
And so it's expecting more indices to be fed when it isn't getting them
