Design Focused:
 - Voxel engine
  - Create chunks (probably look back on the way they are done in handmade hero)
  - Create forward facing geo.
   - Follow jdh guy, he had some stuff about creating an algorithm to determine if a cube was facing forward or not
 - Redo camera system such that it rotates around a fixed point using some sort of cursor movement
 - Eliminate the appearance of voxels depending on if they have been clicked
   - Raycasting system
   - Recognizing hit cubes and hiding them from being rendered
- Create scoring system based on how well the sculpture was created according to the picture
 - Wow what does this even mean I'm talking super reach right here

Quick and Easy:
 - Fullscreen mode (this was a lie, not quick or easy)
 


Longer, probably not as easy:
 - Create raycasting system (probably would help if they could be debugged too)
 - Eliminate appearance of voxels depending on if they have been clicked



Reaching now:
- Buff out backend to be used for multiple games



TODO:
 - Edit the mesh by clicking and removing voxels


Get an actual reading of DeltaTime, not just the current constant you have in place of it
You'll probably also want to rotate the camera with deltatime

You'll probably want to add some keys for resetting the focus on the cube
Maybe add camera lag?


Problems that I decided to leave until later:
 - Raycasting doesn't line up


Inspirations besides stardew - any non game related?

Note on memory lists:
 - I think what you have, at least for removing items from the list is incorrect
  - Right now, if you use the RemoveFromList it just removes the first thing in the list from the list
   - This is problematic when you have a specific item you want to remove from the list
    - What you will probably have to implement is some sort of system to identify the item being removed
    and probably implement a double linked list
 - Also, your AddToEndOfList function removes the list->next item, which could be problematic if that is important
 in freeing things

But for now just finish the implementation of the index buffer lists and stuff so it at least renders properly
    





0x224acb000a0


base + (1255 * singleNodeSize)

Create a system for spawning things so you can spawn a cube or something debug at the hit location of the ray
 - Create a list of spawnables
 - Create a new draw function
 - Import debug ico from obj file




Where were you:
 - I still need to actually render the objects which requires using the linked nodes created
 and going over them, you need to look at the previous way in which you were rendering to get
 an idea of how to render the individual objects which is different from the way you do voxels
 bc there is no need to use a mapped resource 
